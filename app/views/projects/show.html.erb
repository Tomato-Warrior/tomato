<div class="container">
  <%= link_to "新增任務", new_project_task_path(@project.id) %>

  <ul>
    <% @project.tasks.each do |task| %>
      <li><%= link_to task_tictac_path(task_id: task.id) do %>
              <i class="far fa-play-circle"></i>
          <% end %>
          <%= link_to task.task_name, project_task_path(project_id: task.project.id) %>
      </li>
    <% end %>
  </ul>
</div>